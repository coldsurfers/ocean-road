{
  "name": "@coldsurfers/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate-tokens": "node scripts/generate-tokens.mjs",
    "generate-llms": "node scripts/generate-llms.mjs",
    "prebuild": "pnpm run generate-tokens && pnpm run generate-llms",
    "predev": "pnpm run generate-tokens && pnpm run generate-llms",
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "check": "biome check .",
    "check:type": "tsc --noEmit"
  },
  "dependencies": {
    "rspress": "^1.47.1"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
