{
  "name": "@coldsurfers/docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate-tokens": "node scripts/generate-tokens.mjs",
    "prebuild": "pnpm run generate-tokens",
    "predev": "pnpm run generate-tokens",
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "check": "biome check .",
    "check:type": "tsc --noEmit"
  },
  "dependencies": {
    "rspress": "^1.47.1"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
